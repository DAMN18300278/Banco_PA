@model Banco.Models.Gerente

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Gerente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="NumNom" />
            <div class="form-group">
                <label asp-for="Nomina" class="control-label"></label>
                <select asp-for="Nomina" class="form-control" asp-items="ViewBag.Nomina"></select>
                <span asp-validation-for="Nomina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DiasVacaciones" class="control-label"></label>
                <input asp-for="DiasVacaciones" class="form-control" />
                <span asp-validation-for="DiasVacaciones" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
