@model IEnumerable<Banco.Models.Gerente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiasVacaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NominaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiasVacaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NominaNavigation.Nomina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumNom">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumNom">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumNom">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
